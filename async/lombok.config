# Without this, @Qualifier on a field is dropped from the constructor Lombok generates, and the
# injection of the two ThreadPoolTaskExecutor beans would only work by accident, through the
# parameter name matching the bean name.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
